captcha==0.2.4  # TODO: check if this one is needed
certifi==2018.1.18
chardet==3.0.4
dj-database-url==0.5.0
dj-static==0.0.6
Django==1.11.12  # use django 2.X for python 3
django-recaptcha==1.4.0
gunicorn==19.7.1
idna==2.6
loremipsum==1.0.5
Pillow==8.1.1  # pillo is for images, not sure we need it her
psycopg2==2.7.4  # this is only needed for heroku
psycopg2-binary==2.7.4i # this is only needed for heroku
pytz==2018.3
requests==2.18.4
selenium==3.11.0  # selenium is for testing, not needed in production server
static3==0.7.0
urllib3==1.22
